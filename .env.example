# Local development works without an .env file. These values document the
# runtime contract used by the production container and Hawk Suite.
HOST=127.0.0.1
PORT=3000
DATA_DIR=./data
APP_URL=http://localhost:3000
LOG_LEVEL=info
